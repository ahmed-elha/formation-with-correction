export class Note {
    public coefficient: number;
    public note: number;

    constructor(note: number, coefficient: number) {
        this.coefficient = coefficient;
        this.note = note;
    }
}
